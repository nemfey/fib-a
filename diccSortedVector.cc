#include <iostream>
#include <cstdlib>
#include <ctime>
#include <string>
#include <vector>

using namespace std;

int main() {
    
    srand(time(NULL));
    int n = rand() % 11 + 10;
    vector<string> words = {"joan","pol","victor","mario"};
}
