#ifndef WORD_SEARCH
#define WORD_SEARCH

using namespace std;

typedef vector<char> Row;
typedef vector<Row> Matrix;

class WordSearch {
    
    private:
        Matrix wordSearch;
        
    public:
        
        //constructora de la sopa de letras
        WordSearch();
}
